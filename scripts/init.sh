#!/bin/bash
# Скрипт инициализации проекта: создание .env и необходимых директорий

set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

echo "🚀 Инициализация проекта DC-Detector..."

# Создание .env файла
"$SCRIPT_DIR/init-env.sh"

# Создание необходимых директорий
echo "📁 Создание директорий..."
mkdir -p "$PROJECT_ROOT/data/detections/saved"
mkdir -p "$PROJECT_ROOT/services/detection/models"

echo "✅ Директории созданы"

# Проверка наличия модели YOLO
MODEL_PATH="$PROJECT_ROOT/services/detection/models/bestfire.pt"
if [ ! -f "$MODEL_PATH" ]; then
    echo "⚠️  Предупреждение: Модель bestfire.pt не найдена в $MODEL_PATH"
    echo "   Убедитесь, что модель YOLO размещена в правильной директории"
    echo "   Или используйте yolov8n.pt (будет загружена автоматически при первом запуске)"
else
    echo "✅ Модель bestfire.pt найдена"
fi

echo "✨ Инициализация завершена"

