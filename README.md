# 🔥 DC-Detector - Модульная система детекции

Модульная система детекции огня на базе Docker и микросервисов.

## 🏗️ Архитектура

Проект разбит на отдельные сервисы, каждый из которых может быть запущен в отдельном Docker контейнере:

### 📷 Camera Service
Универсальный сервис для работы с камерами. Автоматически определяет и использует доступную камеру (PiCamera, Picamera2, USB веб-камера).

## 🚀 Быстрый старт

### Запуск Camera Service

```bash
cd camera-service
docker-compose up -d --build
```

Сервис будет доступен по адресу: http://localhost:8000

### Использование видеопотока

Подключение к видеопотоку из другого приложения:
```html
<img src="http://camera-service:8000/video_feed" alt="Camera Stream">
```

## 📦 Структура проекта

```
DC-Detector/
├── camera-service/          # Сервис камеры
│   ├── camera_server.py     # Основной код сервиса
│   ├── Dockerfile           # Docker образ
│   ├── docker-compose.yml   # Docker Compose конфигурация
│   ├── requirements.txt     # Python зависимости
│   └── README.md            # Документация сервиса
├── archive/                 # Архив старого проекта
│   └── original-project/    # Оригинальный монолитный проект
├── bestfire.pt              # Модель YOLO для детекции огня
├── yolov8n.pt               # Базовая модель YOLOv8
├── requirements.txt         # Общие зависимости
└── README.md                # Этот файл
```

## 🔧 Сервисы

### Camera Service
- **Порт:** 8000
- **API:** `/video_feed`, `/status`, `/health`
- **Поддержка:** PiCamera, Picamera2, USB веб-камеры
- **Формат:** MJPEG видеопоток

См. [camera-service/README.md](camera-service/README.md) для подробной документации.

## 📊 Планируемые сервисы

- 🔍 **Detection Service** - Сервис детекции огня на базе YOLO
- 📊 **API Gateway** - Единая точка входа для всех сервисов
- 💾 **Storage Service** - Хранение записей и метаданных
- 📡 **Notification Service** - Уведомления (email, SMS, push)

## 🎯 Использование

### Разработка

```bash
# Запуск конкретного сервиса
cd camera-service
docker-compose up

# Запуск всех сервисов (когда будут готовы)
docker-compose -f docker-compose.all.yml up
```

### Production

```bash
# Сборка образов
docker-compose build

# Запуск в background
docker-compose up -d

# Просмотр логов
docker-compose logs -f
```

## 📝 API

### Camera Service

- `GET /` - Веб-интерфейс
- `GET /video_feed` - MJPEG видеопоток
- `GET /status` - Статус камеры (JSON)
- `GET /health` - Health check

Пример статуса:
```json
{
  "camera_type": "webcam",
  "running": true,
  "status": "active"
}
```

## 🐳 Docker

Каждый сервис может быть запущен независимо:

```bash
# В директории сервиса
docker-compose up -d

# Или через Docker напрямую
docker build -t camera-service .
docker run -p 8000:8000 --privileged camera-service
```

## 🔒 Безопасность

Для работы с камерами на Raspberry Pi требуется:
```yaml
privileged: true
devices:
  - /dev/video0:/dev/video0
```

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `docker-compose logs`
2. Проверьте статус: `docker-compose ps`
3. Убедитесь, что камера доступна: `lsusb` или `libcamera-hello`

## 📄 Лицензия

MIT License

## 🙏 Благодарности

- Ultralytics за YOLO модель
- Raspberry Pi Foundation
- OpenCV сообщество
