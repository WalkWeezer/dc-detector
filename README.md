# 🔥 DC-Detector - Fire Detection System

Система детекции огня для Raspberry Pi с использованием YOLO и камеры.

## 🚀 Быстрый старт

### Вариант 1: Полная установка (рекомендуется для первого запуска)
```bash
git clone https://github.com/WalkWeezer/dc-detector.git
cd dc-detector
chmod +x setup.sh start.sh
./setup.sh
./start.sh
```

### Вариант 2: Только запуск (если система уже настроена)
```bash
git clone https://github.com/WalkWeezer/dc-detector.git
cd dc-detector
chmod +x start.sh
./start.sh
```

### Веб-интерфейс
Откройте браузер и перейдите по адресу:
- `http://YOUR_PI_IP:5000`
- `http://localhost:5000`

## 📋 Требования

### Оборудование
- Raspberry Pi 3/4 (рекомендуется Pi 4)
- PiCamera v2 или совместимая камера
- MicroSD карта 16GB+ (Class 10)
- Источник питания 5V/3A (для Pi 4)

### Программное обеспечение
- Raspberry Pi OS (Bullseye или новее)
- Python 3.8+
- 2GB+ свободного места на SD карте

## 🔧 Что делают скрипты

### setup.sh - Полная установка:
- ✅ Обновляет систему Raspberry Pi
- ✅ Устанавливает все системные зависимости (OpenCV, libcamera, etc.)
- ✅ Настраивает камеру
- ✅ **Создает виртуальное окружение Python**
- ✅ **Устанавливает все Python зависимости (включая YOLO)**
- ✅ Проверяет корректность установки

### start.sh - Запуск приложения:
- ✅ Проверяет наличие файлов приложения
- ✅ Проверяет виртуальное окружение
- ✅ Активирует виртуальное окружение
- ✅ Проверяет камеру
- ✅ Запускает приложение
- ✅ Показывает адрес веб-интерфейса

## 🎯 Использование

### Простой запуск:

1. **Запуск системы:**
   ```bash
   ./start.sh
   ```

2. **Откройте веб-интерфейс:**
   - `http://YOUR_PI_IP:5000`
   - `http://localhost:5000`

3. **Остановка:**
   - Нажмите `Ctrl+C` в терминале

## 🚨 Устранение неполадок

### Если start.sh не работает:

1. **Если ошибка с torch, libcamera или Python модулями:**
   ```bash
   chmod +x fix_venv.sh
   ./fix_venv.sh
   ```

2. **Или установка совместимых пакетов:**
   ```bash
   chmod +x install_packages.sh
   ./install_packages.sh
   ```

3. **Сначала запустите setup.sh:**
   ```bash
   chmod +x setup.sh
   ./setup.sh
   ```

4. **Проверьте права:**
   ```bash
   chmod +x start.sh
   ```

5. **Проверьте Python:**
   ```bash
   python3 --version
   # Должна быть версия 3.8+
   ```

### Если setup.sh не работает:

1. **Проверьте права:**
   ```bash
   chmod +x setup.sh
   ```

2. **Если ошибка с репозиториями:**
   ```bash
   chmod +x fix_repos.sh
   ./fix_repos.sh
   ```

3. **Запустите с правами sudo:**
   ```bash
   sudo ./setup.sh
   ```

### Если камера не работает:

1. **Включите камеру:**
   ```bash
   sudo raspi-config
   # Interface Options → Camera → Enable
   ```

2. **Проверьте камеру:**
   ```bash
   vcgencmd get_camera
   # Должно показать: supported=1 detected=1
   ```

3. **Тест камеры:**
   ```bash
   libcamera-hello --timeout 2000
   ```

### Если модули не устанавливаются:

1. **Обновите pip:**
   ```bash
   source venv/bin/activate
   pip install --upgrade pip
   ```

2. **Переустановите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

## 📊 Мониторинг

### Системная информация:
```bash
# Температура CPU
vcgencmd measure_temp

# Использование памяти
free -h

# Свободное место
df -h

# Загрузка системы
uptime
```

## 🔒 Безопасность

### Настройка брандмауэра:
```bash
# Разрешить только HTTP трафик
sudo ufw allow 5000/tcp
sudo ufw enable
```

## 📱 Удаленный доступ

### Настройка статического IP:
```bash
# Редактировать /etc/dhcpcd.conf
sudo nano /etc/dhcpcd.conf

# Добавить:
interface eth0
static ip_address=192.168.1.100/24
static routers=192.168.1.1
static domain_name_servers=8.8.8.8
```

## 🎉 Готово!

После успешной установки система будет доступна по адресу:
- `http://YOUR_PI_IP:5000`

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в терминале
2. Убедитесь, что камера включена
3. Создайте issue в репозитории с подробным описанием

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

## 🙏 Благодарности

- Ultralytics за YOLO модель
- Raspberry Pi Foundation за отличную платформу
- OpenCV сообщество за компьютерное зрение