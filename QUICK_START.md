# 🚀 Быстрый запуск системы

Созданы исполняемые скрипты для быстрого запуска всей системы:

## Windows

### Вариант 1: Batch файл (рекомендуется)
Просто дважды кликните на файл `start.bat` или запустите из командной строки:
```cmd
start.bat
```

### Вариант 2: PowerShell скрипт
```powershell
.\start.ps1
```

Оба скрипта запустят оба сервиса в отдельных окнах:
- **Camera Service** на порту 8000
- **Detection Service** на порту 8001

## Linux / Raspberry Pi

Сделайте скрипт исполняемым (первый раз):
```bash
chmod +x start.sh
```

Запустите:
```bash
./start.sh
```

Для остановки нажмите `Ctrl+C` в терминале.

## Доступ к сервисам

После запуска откройте в браузере:

- **Camera Service**: http://localhost:8000
- **Detection Service**: http://localhost:8001

## Требования

Перед запуском убедитесь, что установлены все зависимости:

### Camera Service
```bash
cd camera-service
pip install -r requirements.txt
```

### Detection Service
```bash
cd detection-service
pip install -r requirements.txt
```

**Важно для Raspberry Pi**: Detection Service требует дополнительную установку PyTorch (см. `detection-service/README.md`)

## Ручной запуск (если скрипты не работают)

Если скрипты не работают, вы можете запустить сервисы вручную в двух отдельных терминалах:

**Терминал 1 - Camera Service:**
```bash
cd camera-service
python camera_server.py
```

**Терминал 2 - Detection Service:**
```bash
cd detection-service
python detection_server.py
```

## Остановка

- **Windows (batch)**: Закройте оба окна или нажмите `Ctrl+C` в каждом
- **Windows (PowerShell)**: Нажмите `Ctrl+C`
- **Linux**: Нажмите `Ctrl+C` в терминале

