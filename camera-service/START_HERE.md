# 🚀 Быстрый старт Camera Service

## Что это?

Camera Service - это универсальный сервис для работы с камерами. Он автоматически определяет доступный тип камеры и предоставляет видеопоток через HTTP.

## ✨ Возможности

- 🔄 Автоматическое определение камеры (PiCamera → Picamera2 → USB веб-камера)
- 📸 Поддержка PiCamera (legacy)
- 📷 Поддержка Picamera2  
- 🌐 Поддержка USB веб-камер
- 🎥 MJPEG видеопоток
- 📊 REST API для статуса

## 🚀 Быстрый запуск

### Вариант 1: Docker (рекомендуется)

```bash
cd camera-service
docker-compose up -d --build
```

Сервис будет доступен по адресу: **http://localhost:8000**

### Вариант 2: Нативный запуск (для Raspberry Pi)

```bash
cd camera-service
pip3 install -r requirements.txt
python3 camera_server.py
```

## 📡 API Endpoints

- `GET /` - Веб-интерфейс с видеопотоком
- `GET /video_feed` - MJPEG видеопоток (для встраивания в другие приложения)
- `GET /status` - JSON статус камеры
- `GET /health` - Health check

## 📖 Примеры использования

### Подключение видеопотока в HTML

```html
<img src="http://camera-service:8000/video_feed" alt="Camera Stream">
```

### Проверка статуса через curl

```bash
curl http://localhost:8000/status
```

Ответ:
```json
{
  "camera_type": "webcam",
  "running": true,
  "status": "active"
}
```

## 🔍 Отладка

### Посмотреть логи

```bash
docker-compose logs -f
```

### Проверить статус контейнера

```bash
docker-compose ps
```

### Остановить сервис

```bash
docker-compose down
```

## 📝 Порядок проверки камер

Сервис проверяет камеры в следующем порядке:
1. **PiCamera** (legacy) - старая библиотека для Raspberry Pi
2. **Picamera2** - новая библиотека для Raspberry Pi  
3. **USB WebCam** - обычные веб-камеры через OpenCV

Это гарантирует, что на Raspberry Pi будет использована PiCamera, а на других системах - USB веб-камера.

## ⚠️ Важно

На Raspberry Pi для работы камеры нужно:
- Включить камеру: `sudo raspi-config` → Interface Options → Camera → Enable
- Проверить камеру: `libcamera-hello --timeout 2000`

## 🎯 Что дальше?

Этот сервис - основа для других сервисов:
- **Detection Service** - будет использовать видеопоток для детекции огня
- **API Gateway** - будет проксировать запросы к камере
- **Storage Service** - будет сохранять записи при обнаружении

