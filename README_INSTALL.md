# 🔧 DC-Detector Installation Guide

Руководство по установке системы детекции огня для Raspberry Pi.

## 📋 Доступные скрипты установки

### 1. **setup.sh** - Полная установка (рекомендуется)
- Устанавливает все зависимости
- Максимальная функциональность
- Может иметь проблемы с устаревшими пакетами

### 2. **setup_simple.sh** - Упрощенная установка
- Убирает проблемные пакеты
- Более стабильная работа
- Рекомендуется при ошибках

### 3. **setup_minimal.sh** - Минимальная установка
- Только необходимые компоненты
- Быстрая установка
- Для тестирования

## 🚀 Рекомендуемый порядок установки

### Шаг 1: Попробуйте полную установку
```bash
chmod +x setup.sh
./setup.sh
```

### Шаг 2: Если есть ошибки, используйте упрощенную
```bash
chmod +x setup_simple.sh
./setup_simple.sh
```

### Шаг 3: Если все еще есть проблемы, используйте минимальную
```bash
chmod +x setup_minimal.sh
./setup_minimal.sh
```

## 🔍 Устранение ошибок

### Ошибка: "Unable to locate package libjasper-dev"
**Решение:** Используйте `setup_simple.sh` или `setup_minimal.sh`

### Ошибка: "Package 'libqtgui4' has no installation candidate"
**Решение:** Используйте `setup_simple.sh` или `setup_minimal.sh`

### Ошибка: "Unable to locate libqt4-test"
**Решение:** Используйте `setup_simple.sh` или `setup_minimal.sh`

### Ошибка: "No module named cv2"
**Решение:**
```bash
source fire_detection_env/bin/activate
pip install opencv-python-headless
```

### Ошибка: "No module named ultralytics"
**Решение:**
```bash
source fire_detection_env/bin/activate
pip install ultralytics
```

### Ошибка: "No module named picamera2"
**Решение:**
```bash
source fire_detection_env/bin/activate
pip install picamera2
```

## 🎯 Быстрая установка (если все скрипты не работают)

### Ручная установка:
```bash
# 1. Обновите систему
sudo apt update

# 2. Установите основные пакеты
sudo apt install -y python3-pip python3-venv python3-dev python3-opencv libopencv-dev git

# 3. Включите камеру
sudo raspi-config nonint do_camera 0

# 4. Создайте виртуальное окружение
python3 -m venv fire_detection_env
source fire_detection_env/bin/activate

# 5. Установите Python зависимости
pip install --upgrade pip
pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
pip install ultralytics opencv-python-headless flask numpy pillow psutil picamera2 gpiozero

# 6. Запустите систему
python app_pi.py
```

## 🔧 Проверка установки

### После установки запустите:
```bash
./check_system.sh
```

### Проверьте камеру:
```bash
vcgencmd get_camera
# Должно показать: supported=1 detected=1
```

### Проверьте модули:
```bash
source fire_detection_env/bin/activate
python -c "import cv2, ultralytics, flask, picamera2; print('Все модули работают!')"
```

## 🚨 Если ничего не помогает

### Полная переустановка:
```bash
# Удалите старое окружение
rm -rf fire_detection_env

# Удалите старые файлы
rm -f *.sh

# Клонируйте репозиторий заново
git clone https://github.com/YOUR_USERNAME/DC-Detector.git
cd DC-Detector

# Запустите минимальную установку
chmod +x setup_minimal.sh
./setup_minimal.sh
```

## 📊 Сравнение скриптов

| Скрипт | Зависимости | Время | Стабильность | Рекомендация |
|--------|-------------|-------|--------------|--------------|
| setup.sh | Все | Долго | Средняя | Для новых Pi |
| setup_simple.sh | Большинство | Средне | Высокая | **Рекомендуется** |
| setup_minimal.sh | Минимум | Быстро | Очень высокая | Для старых Pi |

## 🎉 После успешной установки

### Запуск системы:
```bash
./start_fire_detection.sh
```

### Веб-интерфейс:
- `http://YOUR_PI_IP:5000`
- `http://localhost:5000`

### Управление:
```bash
./start_fire_detection.sh    # Запуск
./stop_fire_detection.sh     # Остановка
./status_fire_detection.sh    # Статус
./logs_fire_detection.sh     # Логи
```

## 📞 Поддержка

Если возникли проблемы:
1. Запустите `./check_system.sh` для диагностики
2. Проверьте логи: `journalctl -u fire-detection -f`
3. Создайте issue в репозитории с подробным описанием

## 🔄 Обновление

### Для обновления системы:
```bash
git pull origin main
./setup_simple.sh
```

### Для обновления зависимостей:
```bash
source fire_detection_env/bin/activate
pip install --upgrade -r requirements.txt
```
