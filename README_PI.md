# 🔥 Fire Detection System для Raspberry Pi

Система детекции огня, оптимизированная для работы на Raspberry Pi с PiCamera.

## 📋 Требования

### Оборудование
- Raspberry Pi 3/4 (рекомендуется Pi 4)
- PiCamera v2 или совместимая камера
- MicroSD карта 16GB+ (Class 10)
- Источник питания 5V/3A (для Pi 4)

### Программное обеспечение
- Raspberry Pi OS (Bullseye или новее)
- Python 3.8+
- 2GB+ свободного места на SD карте

## 🚀 Быстрая установка

### 1. Подготовка Raspberry Pi
```bash
# Обновите систему
sudo apt update && sudo apt upgrade -y

# Включите камеру
sudo raspi-config
# Выберите: Interface Options → Camera → Enable
```

### 2. Клонирование репозитория
```bash
git clone https://github.com/YOUR_USERNAME/DC-Detector.git
cd DC-Detector
```

### 3. Автоматическая установка
```bash
chmod +x install_pi.sh
./install_pi.sh
```

### 4. Ручная установка (альтернатива)
```bash
# Создание виртуального окружения
python3 -m venv fire_detection_env
source fire_detection_env/bin/activate

# Установка зависимостей
pip install -r requirements.txt

# Запуск системы
python app_pi.py
```

## 🎯 Использование

### Запуск системы
```bash
# Ручной запуск
./start_fire_detection.sh

# Автозапуск при загрузке
sudo systemctl enable fire-detection
sudo systemctl start fire-detection
```

### Веб-интерфейс
Откройте браузер и перейдите по адресу:
- `http://YOUR_PI_IP:5000` (с другого устройства)
- `http://localhost:5000` (с самого Pi)

### Управление системой
```bash
# Проверка статуса
sudo systemctl status fire-detection

# Остановка
sudo systemctl stop fire-detection

# Перезапуск
sudo systemctl restart fire-detection

# Просмотр логов
journalctl -u fire-detection -f
```

## ⚙️ Конфигурация

### Основные настройки (config_pi.py)
```python
# Настройки камеры
CAMERA = {
    'width': 640,
    'height': 480,
    'fps': 15,
}

# Настройки детекции
DETECTION = {
    'confidence_threshold': 0.3,
    'detection_interval': 3,
}
```

### Оптимизация производительности
Система автоматически определяет модель Raspberry Pi и применяет оптимальные настройки:

- **Pi 4**: FPS=20, 4 потока, высокая производительность
- **Pi 3**: FPS=15, 2 потока, средняя производительность  
- **Pi Zero**: FPS=10, 1 поток, экономия ресурсов

## 🔧 Устранение неполадок

### Проблемы с камерой
```bash
# Проверка камеры
vcgencmd get_camera
# Должно показать: supported=1 detected=1

# Тест камеры
libcamera-hello --timeout 2000
```

### Проблемы с производительностью
```bash
# Мониторинг ресурсов
htop
# Температура CPU
vcgencmd measure_temp
```

### Проблемы с зависимостями
```bash
# Переустановка OpenCV
pip uninstall opencv-python
pip install opencv-python-headless

# Установка дополнительных библиотек
sudo apt install libhdf5-dev libhdf5-serial-dev
```

## 📊 Мониторинг системы

### Системная информация
Веб-интерфейс отображает:
- Статус камеры
- Статус детекции
- Температуру CPU
- Использование CPU/RAM
- Количество обнаружений

### Логи
```bash
# Просмотр логов в реальном времени
tail -f logs/fire_detection.log

# Системные логи
journalctl -u fire-detection --since "1 hour ago"
```

## 🔒 Безопасность

### Настройка брандмауэра
```bash
# Разрешить только HTTP трафик
sudo ufw allow 5000/tcp
sudo ufw enable
```

### Настройка SSH (опционально)
```bash
# Отключить SSH если не нужен
sudo systemctl disable ssh
```

## 📱 Удаленный доступ

### Настройка статического IP
```bash
# Редактировать /etc/dhcpcd.conf
sudo nano /etc/dhcpcd.conf

# Добавить:
interface eth0
static ip_address=192.168.1.100/24
static routers=192.168.1.1
static domain_name_servers=8.8.8.8
```

### Настройка Wake-on-LAN (для Pi 4)
```bash
# Включить Wake-on-LAN
echo 'wol g' | sudo tee -a /boot/config.txt
```

## 🚨 Аварийные процедуры

### Экстренная остановка
```bash
# Остановка всех процессов детекции
sudo pkill -f fire_detection
sudo systemctl stop fire-detection
```

### Сброс к заводским настройкам
```bash
# Удаление всех данных
sudo systemctl stop fire-detection
sudo systemctl disable fire-detection
sudo rm -rf /etc/systemd/system/fire-detection.service
sudo systemctl daemon-reload
```

## 📈 Производительность

### Оптимизация для максимальной производительности
1. Используйте быструю SD карту (Class 10+)
2. Увеличьте GPU память: `sudo raspi-config` → Advanced → Memory Split → 128
3. Отключите неиспользуемые сервисы
4. Используйте активное охлаждение для Pi 4

### Мониторинг ресурсов
```bash
# Скрипт мониторинга
#!/bin/bash
while true; do
    echo "$(date): CPU: $(vcgencmd measure_temp), Load: $(cat /proc/loadavg)"
    sleep 60
done
```

## 🤝 Поддержка

### Получение помощи
1. Проверьте логи: `journalctl -u fire-detection`
2. Проверьте статус камеры: `vcgencmd get_camera`
3. Создайте issue в репозитории с подробным описанием проблемы

### Полезные команды
```bash
# Информация о системе
cat /proc/cpuinfo | grep Model
cat /proc/version

# Информация о камере
libcamera-hello --list-cameras

# Проверка портов
netstat -tlnp | grep :5000
```

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл LICENSE для подробностей.

## 🙏 Благодарности

- Ultralytics за YOLO модель
- Raspberry Pi Foundation за отличную платформу
- OpenCV сообщество за компьютерное зрение
