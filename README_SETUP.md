# 🚀 DC-Detector Auto Setup

Автоматическая установка и настройка системы детекции огня для Raspberry Pi.

## 📋 Быстрый старт

### 1. **Автоматическая установка (рекомендуется)**
```bash
# Сделайте скрипт исполняемым
chmod +x setup.sh

# Запустите автоматическую установку
./setup.sh
```

### 2. **Быстрый запуск системы**
```bash
# После установки запустите систему
chmod +x quick_start.sh
./quick_start.sh
```

### 3. **Проверка системы**
```bash
# Проверьте состояние системы
chmod +x check_system.sh
./check_system.sh
```

## 🔧 Что делает setup.sh

### Автоматически устанавливает:
- ✅ Обновляет систему Raspberry Pi
- ✅ Устанавливает все системные зависимости
- ✅ Настраивает камеру (PiCamera)
- ✅ Создает виртуальное окружение Python
- ✅ Устанавливает все Python зависимости
- ✅ Настраивает автозапуск через systemd
- ✅ Создает скрипты управления
- ✅ Настраивает права доступа

### Создает скрипты управления:
- `start_fire_detection.sh` - Запуск системы
- `stop_fire_detection.sh` - Остановка системы
- `status_fire_detection.sh` - Статус системы
- `restart_fire_detection.sh` - Перезапуск системы
- `logs_fire_detection.sh` - Просмотр логов

## 🎯 Использование

### После установки:

1. **Запуск системы:**
   ```bash
   ./start_fire_detection.sh
   ```

2. **Откройте веб-интерфейс:**
   - `http://YOUR_PI_IP:5000`
   - `http://localhost:5000`

3. **Управление сервисом:**
   ```bash
   # Запуск
   sudo systemctl start fire-detection
   
   # Остановка
   sudo systemctl stop fire-detection
   
   # Статус
   sudo systemctl status fire-detection
   
   # Логи
   journalctl -u fire-detection -f
   ```

## 🔍 Проверка системы

### Скрипт check_system.sh проверяет:
- ✅ Информацию о системе
- ✅ Состояние камеры
- ✅ Виртуальное окружение
- ✅ Установленные модули
- ✅ Файлы проекта
- ✅ Systemd сервис
- ✅ Ресурсы системы
- ✅ Сетевое соединение

### Запуск проверки:
```bash
./check_system.sh
```

## 🚨 Устранение неполадок

### Если setup.sh не работает:

1. **Проверьте права:**
   ```bash
   chmod +x setup.sh
   ```

2. **Запустите с правами sudo:**
   ```bash
   sudo ./setup.sh
   ```

3. **Проверьте логи:**
   ```bash
   tail -f /var/log/syslog
   ```

### Если камера не работает:

1. **Включите камеру:**
   ```bash
   sudo raspi-config
   # Interface Options → Camera → Enable
   ```

2. **Проверьте камеру:**
   ```bash
   vcgencmd get_camera
   # Должно показать: supported=1 detected=1
   ```

3. **Тест камеры:**
   ```bash
   libcamera-hello --timeout 2000
   ```

### Если модули не устанавливаются:

1. **Обновите pip:**
   ```bash
   source fire_detection_env/bin/activate
   pip install --upgrade pip
   ```

2. **Переустановите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Проверьте Python версию:**
   ```bash
   python3 --version
   # Должна быть 3.8+
   ```

## 📊 Мониторинг

### Системная информация:
```bash
# Температура CPU
vcgencmd measure_temp

# Использование памяти
free -h

# Свободное место
df -h

# Загрузка системы
uptime
```

### Логи системы:
```bash
# Логи fire-detection
journalctl -u fire-detection -f

# Системные логи
tail -f /var/log/syslog
```

## 🔒 Безопасность

### Настройка брандмауэра:
```bash
# Разрешить только HTTP трафик
sudo ufw allow 5000/tcp
sudo ufw enable
```

### Настройка SSH:
```bash
# Отключить SSH если не нужен
sudo systemctl disable ssh
```

## 📱 Удаленный доступ

### Настройка статического IP:
```bash
# Редактировать /etc/dhcpcd.conf
sudo nano /etc/dhcpcd.conf

# Добавить:
interface eth0
static ip_address=192.168.1.100/24
static routers=192.168.1.1
static domain_name_servers=8.8.8.8
```

## 🎉 Готово!

После успешной установки система будет доступна по адресу:
- `http://YOUR_PI_IP:5000`

Для управления используйте созданные скрипты или systemd команды.

## 📞 Поддержка

Если возникли проблемы:
1. Запустите `./check_system.sh` для диагностики
2. Проверьте логи: `journalctl -u fire-detection -f`
3. Создайте issue в репозитории с подробным описанием
