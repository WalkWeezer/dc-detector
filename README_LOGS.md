# 📄 DC-Detector Logs Management

Руководство по работе с логами установки и отладке проблем.

## 🔍 Автоматическое логирование

### Что логируется:
- ✅ Все команды установки
- ✅ Вывод команд и ошибки
- ✅ Системная информация
- ✅ Статус каждого этапа
- ✅ Время выполнения

### Где сохраняются логи:
- Файлы: `setup_log_YYYYMMDD_HHMMSS.log`
- Автоматически добавляются в Git репозиторий
- Доступны для анализа и отладки

## 📋 Команды для работы с логами

### 1. **Анализ логов**
```bash
# Показать все логи
./read_logs.sh list

# Показать последний лог
./read_logs.sh latest

# Показать только ошибки
./read_logs.sh errors

# Показать только успешные операции
./read_logs.sh success

# Показать статистику
./read_logs.sh stats
```

### 2. **Коммит логов**
```bash
# Коммит логов локально
./commit_logs.sh

# Коммит и отправить в удаленный репозиторий
./commit_logs.sh --push
```

### 3. **Просмотр конкретного лога**
```bash
# Показать конкретный лог
./read_logs.sh show setup_log_20231201_143022.log

# Просмотр в текстовом редакторе
nano setup_log_20231201_143022.log
```

## 🚨 Анализ проблем

### Типичные ошибки и решения:

#### **Ошибка: "Unable to locate package"**
```bash
# В логе ищите:
grep -i "unable to locate\|package.*not found" setup_log_*.log
```
**Решение:** Обновить список пакетов в setup.sh

#### **Ошибка: "No module named"**
```bash
# В логе ищите:
grep -i "no module named\|import error" setup_log_*.log
```
**Решение:** Проверить установку Python зависимостей

#### **Ошибка: "Camera not detected"**
```bash
# В логе ищите:
grep -i "camera.*not\|vcgencmd" setup_log_*.log
```
**Решение:** Проверить подключение камеры

#### **Ошибка: "Permission denied"**
```bash
# В логе ищите:
grep -i "permission denied\|access denied" setup_log_*.log
```
**Решение:** Проверить права доступа

## 📊 Статистика установки

### Анализ успешности:
```bash
# Общая статистика
./read_logs.sh stats

# Количество ошибок
grep -c -i "error\|failed" setup_log_*.log

# Количество предупреждений
grep -c -i "warning\|warn" setup_log_*.log

# Время установки
grep -i "installation.*time\|setup.*completed" setup_log_*.log
```

### Сравнение логов:
```bash
# Сравнить два лога
diff setup_log_20231201_143022.log setup_log_20231201_150000.log

# Найти различия в ошибках
grep -i "error" setup_log_*.log | sort | uniq
```

## 🔧 Отладка проблем

### 1. **Поиск конкретной ошибки**
```bash
# Найти все ошибки с определенным текстом
grep -i "python3-pyqt5" setup_log_*.log

# Найти ошибки на определенном этапе
grep -A 5 -B 5 "install_camera_dependencies" setup_log_*.log
```

### 2. **Анализ производительности**
```bash
# Время выполнения каждого этапа
grep -i "header.*=" setup_log_*.log

# Команды, которые выполнялись дольше всего
grep -i "executing:" setup_log_*.log
```

### 3. **Проверка системных требований**
```bash
# Информация о системе
grep -A 10 "System info:" setup_log_*.log

# Версии пакетов
grep -i "version\|installed" setup_log_*.log
```

## 📈 Мониторинг установки

### Автоматические отчеты:
- Логи автоматически коммитятся в Git
- Создается отчет с анализом ошибок
- Статистика успешности установки

### Уведомления:
```bash
# Проверить статус последней установки
./read_logs.sh latest | grep -i "status\|error\|success"

# Получить краткий отчет
./read_logs.sh stats
```

## 🎯 Лучшие практики

### 1. **Перед установкой:**
```bash
# Проверить свободное место
df -h

# Проверить подключение к интернету
ping -c 3 8.8.8.8

# Проверить версию системы
cat /etc/os-release
```

### 2. **После установки:**
```bash
# Проверить логи на ошибки
./read_logs.sh errors

# Проверить статус системы
./check_system.sh

# Запустить тесты
./quick_start.sh
```

### 3. **При проблемах:**
```bash
# Сохранить полный лог
cp setup_log_*.log backup_logs/

# Создать отчет о проблеме
./read_logs.sh latest > problem_report.txt

# Отправить в репозиторий для анализа
./commit_logs.sh --push
```

## 📞 Поддержка

### При отправке отчета о проблеме включите:
1. **Лог установки:** `setup_log_*.log`
2. **Системную информацию:** `uname -a`, `cat /etc/os-release`
3. **Статус камеры:** `vcgencmd get_camera`
4. **Свободное место:** `df -h`
5. **Версию Python:** `python3 --version`

### Полезные команды для диагностики:
```bash
# Полная диагностика системы
./check_system.sh > system_diagnosis.txt

# Анализ логов
./read_logs.sh stats > log_analysis.txt

# Создание отчета о проблеме
echo "=== SYSTEM INFO ===" > problem_report.txt
uname -a >> problem_report.txt
cat /etc/os-release >> problem_report.txt
echo "=== LOG ANALYSIS ===" >> problem_report.txt
./read_logs.sh latest >> problem_report.txt
```

## 🎉 Готово!

Теперь у вас есть полная система логирования и отладки для DC-Detector! Все проблемы установки будут отслеживаться и сохраняться в репозитории для анализа.
